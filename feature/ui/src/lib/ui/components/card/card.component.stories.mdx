import {  Meta, Story, Canvas, ArgsTable } from '@storybook/blocks';
import { ButtonComponent } from '../../atoms/button/button.component';
import { CardComponent } from './partials/card.component';
import { CardHeaderComponent } from './partials/card-header.component';
import { CardTitleComponent } from './partials/card-title.component';
import { CardDescriptionComponent } from './partials/card-description.component';
import { CardContentComponent } from './partials/card-content.component';
import { CardFooterComponent } from './partials/card-footer.component';
import { moduleMetadata } from '@storybook/angular';


<Meta title="Components/Card/Card Component" component={CardComponent} decorators={[
  moduleMetadata({
    imports: [
      ButtonComponent,
      CardHeaderComponent,
      CardTitleComponent,
      CardDescriptionComponent,
      CardContentComponent,
      CardFooterComponent,
    ],
  }),

]} />

# Card Component

The `CardComponent` is a reusable component that displays a card.


## Usage

Here's an example of how you can use the `CardComponent`:

<Canvas>
  <Story name="Card Example">
    {{
      template: `
        <Card [cardClass]="'p-4 w-[400px]'">
          <CardHeader>
            <CardTitle [titleClass]="'text-4xl'">Down the Rabbit Hole</CardTitle>
            <CardDescription>
              Alice in Wonderland
              <small>— Lewis Carroll</small>
            </CardDescription>
          </CardHeader>
          <CardContent>
            <p>
              Alice was beginning to get very tired of sitting by her sister on the bank,
              and of having nothing to do: once or twice she had peeped into the book her
              sister was reading, but it had no pictures or conversations in it, “and what
              is the use of a book, ” thought Alice, “without pictures or conversations?”
            </p>
          </CardContent>
          <CardFooter>
            <mantis-button size="lg">Add to bookmarks</mantis-button>
          </CardFooter>
        </Card>
      `
    }}
  </Story>
</Canvas>

## Props

<ArgsTable of={CardComponent} />


